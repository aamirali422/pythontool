{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Zendesk Backup",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/backup.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
